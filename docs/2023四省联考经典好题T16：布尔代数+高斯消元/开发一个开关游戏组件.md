## 让DeepSeek实现九宫格翻转游戏网页版

大佬，你是一名专家前端工程师，精通各种前端技术栈。请帮我开发一个网页，网页上只有一个九宫格，坐标编号从`(0,0)`到`(2,2)`，每个格子标着数字1或0。点击某个格子时，自身和所有相邻的格子改变状态。初始状态是全0，目标状态是`(0,0)`处变成1，其他格子都是0。达到目标状态时弹出提示框，祝贺成功。

要求：

1. 技术栈：React、Tailwind CSS。请输出单个html文件
2. 希望九宫格是绿色系的
3. 除了九宫格以外，不需要任何文字说明

最佳实践：

1. 遵循DRY原则，3次及以上重复出现的代码应抽象为函数、子组件等
2. 缩进为2个空格

没想到现在DeepSeek性能好了那么多！真的做到了保持简洁，绿色系的配色也很清爽！

## 试试让gemini实现Vue组件版

大佬，你是一名专家前端工程师，精通各种前端技术栈。请帮我开发一个Vue组件，组件里只有一个九宫格，坐标编号从`(0,0)`到`(2,2)`，每个格子标着数字1或0。点击某个格子时，自身和所有相邻的格子改变状态。初始状态是全0，目标状态是`(0,0)`处变成1，其他格子都是0。达到目标状态时弹出提示框，祝贺成功。

要求：

1. 技术栈：Vue3、Tailwind CSS。请输出单个vue组件
2. 希望九宫格是绿色系的
3. 除了九宫格以外，不需要任何文字说明

---

发现gemini虽然也正确实现了，但输出的质量比DeepSeek差！配色没那么好看，也没有记录步数和重置按钮。于是继续对话：

大佬，你的实现是正确的。我改了些代码，下面请在我给你的代码的基础上，新增功能：

1. 记录点击格子的步数
2. 新增重置按钮
3. 现在格子大小是`w-8 h-8 sm:w-10 sm:h-10`。希望支持输入格子大小，默认值就是现在的，如果输入的值比较大，就把边界宽度改成`gap-4 p-4`。不太大就保持`gap-2 p-2`
4. 支持自定义格子数，`n*n`，默认n为3

---

gemini用不了，上面的提示词实际没有运行。还是用通义千问。

大佬，你是一名专家前端工程师，精通各种前端技术栈。我有一个vue组件实现了Lights Out游戏。代码在下面给出。下面请在我给你的代码的基础上，新增功能：

1. 记录点击格子的步数
2. 新增重置按钮
3. 现在格子大小是`w-8 h-8 sm:w-10 sm:h-10`。希望支持输入格子大小，默认值就是现在的，如果输入的值比较大，就把边界宽度改成`gap-4 p-4`。不太大就保持`gap-2 p-2`
4. 支持自定义格子数，`n*n`，默认n为3

实现的质量还是OK的，有些小bug，修了就OK
