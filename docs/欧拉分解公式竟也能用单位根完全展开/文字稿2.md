用单位根的定义不难验证这条式子正确，
但我们感兴趣的是怎么从左边得到右边。
搜索网上资讯可知，这涉及n阶循环矩阵。
不难验证3阶循环矩阵的行列式是式子左边，
我们只需要用一些高等代数知识，
证明行列式也能用式子右边来表示就行。

一般的循环矩阵难以研究，
我们不妨先研究基础循环矩阵，
再用基础循环矩阵拼出一般循环矩阵。
基础循环矩阵可以用分块矩阵表示，
其右上角和左下角分别是n减1阶和1阶。
同学们动手算算n=3的基础循环矩阵吧！
接着来看看基础循环矩阵的性质。
P的k次幂仍然能用分块矩阵表示，
其右上角和左下角分别是n减k阶和k阶。
特别地，P的n次幂回到了单位阵。

为了理解刚刚提到的性质，
下面来看看左乘P对方阵A的作用。
我们知道A可以看成n个列向量，
所以只需要看左乘P对列向量的作用。
我们算出，列向量的第2到n个分量向上移动一行，
第1个分量移动到最后一行。
所以总的作用就是A进行一次向上循环移位操作。
动手试试：右乘P对方阵A的作用。

我们知道循环矩阵的行列式可以用特征值乘积表示，
所以只要求出基础循环矩阵的特征值，
再用基础循环矩阵的特征值表示出一般循环矩阵的特征值，
就能完成证明。
P的特征值满足λ的n次方等于1，
所以P的特征值正是n次单位根。
求特征向量对于证明来说没有必要，
但为了知识完整性，还是展示出来，
感兴趣的同学可以看看。

顺便说句题外话，
如果你学过《数值分析》中的离散傅里叶变换，
那么你会发现：P的特征向量刚好就是DFT矩阵的第k列！

我们回头看看一般循环矩阵C的长相，
和基础循环矩阵的幂的长相，
发现c0乘单位阵构成C的对角线，
c1乘P构成C的所有c1，
c2乘P的平方构成C的所有c2，
依此类推，我们就发现，
一般循环矩阵可以用基础循环矩阵的多项式表示出来！
但为了通过这个观察求出C的特征值，
我们还需要引入下面的定理。

这里的命题1我就不读了，很符合直觉。
为了证明命题1，需要引入引理1，
这个也很符合直觉，但严谨的书写需要数学归纳法。

然后用引理1证明命题1。
这里的书写也是照顾严谨性，
其实很符合直觉的，不赘述。

最后用命题1给证明收个尾。
我们不难写出这3个特征值，
于是由特征值之积=行列式，
我们完成了证明。
这个证明是不是很精彩呢？
最后同学们可以动手算下，
n=2可以导出什么恒等式呢？
n=4呢？

这里是我串起整个证明思路的过程中用到的提示词，
感兴趣的同学可以自取~
不过LLM很可能会跳步，也有说车轱辘话的情况，
需要警惕！

我是汉斯，我正致力于为做题人的精神自留地添砖加瓦。
如果你觉得这个视频的解题思路足够清晰自然，
对注意力涣散人群友好，
别忘了点赞、收藏、关注哦。
谢谢观看~
